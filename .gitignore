# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Node (not used, but just in case)
node_modules/

# Supabase local artifacts
supabase/.temp/
supabase/.branches/

# Secrets — never commit (the Gemini key lives in Supabase secrets, not here)
.env
.env.*
*.local
